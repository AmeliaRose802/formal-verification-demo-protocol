// Auto-generated spec for: _ZN4sdep20canonicalize_headers17h3d4a55b799bd297cE
// Constraints derived from type system and annotations
// WARNING: Function may throw -- exception path not modeled

let sdep__canonicalize_headers_b3d6e568_auto_spec : LLVMSetup () = do {

    // Parameter: _0
    _0_ptr <- llvm_alloc (llvm_alias "ptr sret([24 x i8])");
    _0_val <- llvm_fresh_var "_0" (llvm_alias "ptr sret([24 x i8])");
    llvm_points_to _0_ptr (llvm_term _0_val);
    // noalias -- does not alias other params;
    // dereferenceable(24) bytes;

    // Parameter: headers
    headers_ptr <- llvm_alloc_readonly (llvm_int 8);
    headers_before <- llvm_fresh_var "headers" (llvm_int 8);
    llvm_points_to headers_ptr (llvm_term headers_before);
    // NOTE: headers may be null -- spec assumes non-null;
    // noalias -- does not alias other params;
    // dereferenceable(48) bytes;

    llvm_execute_func [_0_ptr, headers_ptr];

    // Postconditions
    llvm_points_to headers_ptr (llvm_term headers_before);
};

// Usage: ov_sdep__canonicalize_headers_b3d6e568 <- llvm_verify m "_ZN4sdep20canonicalize_headers17h3d4a55b799bd297cE" [] false sdep__canonicalize_headers_b3d6e568_auto_spec z3;
