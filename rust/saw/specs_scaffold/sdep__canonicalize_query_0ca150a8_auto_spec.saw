// Auto-generated spec for: _ZN4sdep18canonicalize_query17h3067b6ab6b4a35b7E
// Constraints derived from type system and annotations
// WARNING: Function may throw -- exception path not modeled

let sdep__canonicalize_query_0ca150a8_auto_spec : LLVMSetup () = do {

    // Parameter: _0
    _0_ptr <- llvm_alloc (llvm_alias "ptr sret([24 x i8])");
    _0_val <- llvm_fresh_var "_0" (llvm_alias "ptr sret([24 x i8])");
    llvm_points_to _0_ptr (llvm_term _0_val);
    // noalias -- does not alias other params;
    // dereferenceable(24) bytes;

    // Parameter: query
    query_ptr <- llvm_alloc_readonly (llvm_int 8);
    query_before <- llvm_fresh_var "query" (llvm_int 8);
    llvm_points_to query_ptr (llvm_term query_before);
    // NOTE: query may be null -- spec assumes non-null;
    // noalias -- does not alias other params;
    // dereferenceable(48) bytes;

    llvm_execute_func [_0_ptr, query_ptr];

    // Postconditions
    llvm_points_to query_ptr (llvm_term query_before);
};

// Usage: ov_sdep__canonicalize_query_0ca150a8 <- llvm_verify m "_ZN4sdep18canonicalize_query17h3067b6ab6b4a35b7E" [] false sdep__canonicalize_query_0ca150a8_auto_spec z3;
