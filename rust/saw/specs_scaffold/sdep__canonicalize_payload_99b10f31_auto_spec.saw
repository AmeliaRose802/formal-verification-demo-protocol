// Auto-generated spec for: _ZN4sdep20canonicalize_payload17h131234c17a34d0f4E
// Constraints derived from type system and annotations
// WARNING: Function may throw -- exception path not modeled

let sdep__canonicalize_payload_99b10f31_auto_spec : LLVMSetup () = do {

    // Parameter: _0
    _0_ptr <- llvm_alloc (llvm_alias "ptr sret([24 x i8])");
    _0_val <- llvm_fresh_var "_0" (llvm_alias "ptr sret([24 x i8])");
    llvm_points_to _0_ptr (llvm_term _0_val);
    // noalias -- does not alias other params;
    // dereferenceable(24) bytes;

    // Parameter: request
    request_ptr <- llvm_alloc_readonly (llvm_int 8);
    request_before <- llvm_fresh_var "request" (llvm_int 8);
    llvm_points_to request_ptr (llvm_term request_before);
    // NOTE: request may be null -- spec assumes non-null;
    // noalias -- does not alias other params;
    // dereferenceable(168) bytes;

    llvm_execute_func [_0_ptr, request_ptr];

    // Postconditions
    llvm_points_to request_ptr (llvm_term request_before);
};

// Usage: ov_sdep__canonicalize_payload_99b10f31 <- llvm_verify m "_ZN4sdep20canonicalize_payload17h131234c17a34d0f4E" [] false sdep__canonicalize_payload_99b10f31_auto_spec z3;
