// Auto-generated spec for: _ZN4sdep13provision_key17hdf81303417299a11E
// Constraints derived from type system and annotations
// WARNING: Function may throw -- exception path not modeled

let sdep__provision_key_4159d528_auto_spec : LLVMSetup () = do {

    // Parameter: fleet_enabled
    fleet_enabled <- llvm_fresh_var "fleet_enabled" (llvm_int 1);

    // Parameter: valid_request
    valid_request <- llvm_fresh_var "valid_request" (llvm_int 1);

    // Parameter: vault_result
    vault_result <- llvm_fresh_var "vault_result" (llvm_int 8);

    // Parameter: key_is_active
    key_is_active <- llvm_fresh_var "key_is_active" (llvm_int 1);

    llvm_execute_func [llvm_term fleet_enabled, llvm_term valid_request, llvm_term vault_result, llvm_term key_is_active];

    ret <- llvm_fresh_var "ret" (llvm_int 8);
    // ACTION REQUIRED: This spec only proves absence of undefined behavior.
    // For functional correctness, import your Cryptol spec and replace ret:
    //   import "my_spec.cry";
    //   llvm_return (llvm_term {{ my_spec x : [8] }});
    llvm_return (llvm_term ret);
};

// Usage: ov_sdep__provision_key_4159d528 <- llvm_verify m "_ZN4sdep13provision_key17hdf81303417299a11E" [] false sdep__provision_key_4159d528_auto_spec z3;
